# 加算ナビ（仮）MVP設計書 — 加算取りこぼし診断ツール

作成日: 2026-06-10
フェーズ: MVP（知人テスト用）→ フィードバックを受けて改良

## 1. MVPのゴールと割り切り

**ゴール**: 知り合いの薬剤師・薬局経営者 5〜10人に使ってもらい、
「入力を完走できるか」「金額に納得感があるか」「月額版に何があれば払うか」を検証する。

**やること**
- 月次の集計値を入力 → 算定できている加算 / 取りこぼしている加算 / 機会損失額（概算）を表示
- 結果画面からメールアドレス登録（改定情報の通知を口実にリード獲得）
- 入力〜結果到達のログ収集（どこで離脱したか）

**やらないこと（Phase 2 以降）**
- 会員登録・ログイン
- 決済（Stripe）
- LLM によるリアルタイム解説（解説文は自分が書いた静的テキストで開始。
  制度の解説はドメイン知識が本体価値なので、最初は人力の方が品質も信頼も高い）
- レセコン連携・患者情報の取り扱い（恒久的にやらない方針）

**コスト**: Vercel無料枠 + Supabase無料枠 + ドメイン代（年約1,500円）= 実質ゼロ円運用。

## 2. 技術構成

| レイヤ | 採用技術 | 理由 |
|---|---|---|
| フロント/ホスティング | Next.js (App Router, TypeScript) + Tailwind CSS / Vercel | 一人開発の定番。無料枠で十分。スマホ対応必須 |
| 診断ロジック | クライアントサイドのルールエンジン（TypeScript + JSONマスタ） | 点数計算は決定論的であるべき。LLM不使用で誤情報リスクとAPIコストをゼロに |
| データ保存 | Supabase（Postgres, anon key + RLS） | 診断ログとメールだけ。無料枠で数万件いける |
| 分析 | Vercel Analytics（+ 必要ならGA4） | 完走率の計測が目的 |
| フィードバック収集 | 結果画面に埋め込みフォーム（Supabaseに保存）+ 個別ヒアリング | 知人フェーズは対話が主、フォームは補助 |

### 加算マスタ（このプロダクトの本体資産）

加算の定義はコードから分離した data/kasan-master.ts に集約。
疑義解釈や改定が出たらここだけ直せば全画面に反映される構造にする。

```ts
type Requirement =
  | { id: string; label: string; type: "threshold"; field: InputField; min: number; unit: string }
  | { id: string; label: string; type: "boolean"; field: InputField };

type Kasan = {
  id: string;
  name: string;            // 例: 地域支援・医薬品供給対応体制加算
  points: number;          // 点数
  per: "receipt" | "patient" | "event"; // 算定単位
  eventRate?: number;      // per=event のときの発生率仮定（例: 残薬調整 = 受付の3%）
  requirements: Requirement[];
  advice: string;          // 不足時の改善アドバイス（自分で執筆）
  sourceUrl: string;       // 根拠（日薬PDF・厚労省通知へのリンク）
  lastReviewed: string;    // 自分が内容を確認した日付
};
```

MVPに載せる加算（4〜6個に絞る。網羅より説得力）:
1. 地域支援・医薬品供給対応体制加算
2. 服薬管理指導料1のイ（かかりつけ）
3. 調剤時残薬調整加算
4. 調剤ベースアップ評価料
5. （余力があれば）在宅関連、後発医薬品関連

**点数・要件の正確性はユーザー（=自分）が日薬資料と突き合わせて確認し、lastReviewed を更新する運用。**

### データモデル（Supabase）

```sql
-- 診断ログ（個人情報なし。入力値と結果のみ）
create table diagnoses (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz default now(),
  inputs jsonb not null,      -- { rx: 1500, zaitaku: 10, ... }
  results jsonb not null,     -- { lostYen: 480000, byKasan: [...] }
  source text                 -- 流入元タグ（知人テストは "friends"）
);

-- リード
create table leads (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz default now(),
  email text not null,
  diagnosis_id uuid references diagnoses(id),
  wants_interview boolean default false   -- ヒアリング協力OKか
);

-- フィードバック
create table feedback (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz default now(),
  diagnosis_id uuid references diagnoses(id),
  believable int,             -- 金額の納得感 1-5
  would_pay text,             -- 月額版に何があれば払うか（自由記述）
  comment text
);
```

RLS: insert のみ匿名許可、select は不可（管理は Supabase ダッシュボードで見る）。

## 3. 画面一覧

| # | パス | 画面 | 内容 |
|---|---|---|---|
| 1 | `/` | トップ（LP） | 「あなたの薬局、加算を取りこぼしていませんか？」+ 診断開始CTA。運営者プロフィール（薬局業界出身であることを明記 = 信頼の核） |
| 2 | `/shindan` | 診断入力 | 集計値の入力。スマホはステップ式（1画面1〜2問）、PCは1画面フォーム。「だいたいでOK」の選択肢を必ず用意（正確な数字を知らない人が多い） |
| 3 | `/shindan/kekka` | 結果 | ①取りこぼし額/月（最重要数字を最上部に）②加算ごとのカード（算定中/あと一歩/未達 + 不足要件と改善アドバイス）③根拠リンク ④メール登録CTA ⑤フィードバックフォーム |
| 4 | `/touroku-kanryo` | 登録完了 | お礼 + 「ヒアリング協力可」チェック誘導 |
| 5 | `/legal` | 免責・運営者情報 | **重要**: 本ツールは制度情報の提供であり、個別の算定可否を保証・断定するものではない。最終判断は薬局および地方厚生局への確認による旨を明記 |

結果は inputs を URLクエリにエンコードして共有可能にする（個人情報を含まないので安全。
「うちの結果見て」と経営者仲間に送られるのが理想の拡散経路）。

## 4. 開発ステップ（約2週間）

### Step 0: 加算マスタの執筆（2〜3日・最重要）
- 日薬の点数表PDFと厚労省通知を見ながら、対象4〜6加算の要件・点数・アドバイス文を kasan-master.ts に書く
- これはコーディングではなくドメイン知識の文書化。ここの品質がプロダクトの品質
- 完了条件: 各加算に sourceUrl と lastReviewed が入っている

### Step 1: 計算エンジン + テスト（1〜2日）
- `create-next-app` でプロジェクト作成、Tailwind設定
- lib/engine.ts: inputs → 加算ごとの判定（met / near / unmet）+ 金額計算
- Vitest でユニットテスト（境界値: 在宅23件/24件、後発品率79%/80% など）
- 完了条件: テスト緑。**金額計算にバグがあると信頼が一発で死ぬので、ここだけは手を抜かない**

### Step 2: 画面実装（3〜4日）
- 入力フォーム（スマホ最優先。知人テストはほぼスマホで来る）
- 結果画面（取りこぼし額 → カード一覧 → CTA の順）
- LP・免責ページ
- 完了条件: スマホで入力開始から結果まで2分以内

### Step 3: 計測とデプロイ(1〜2日)
- Supabase 接続（診断ログ・リード・フィードバックの insert）
- Vercel デプロイ + 独自ドメイン + OGP画像（共有時の見栄え）
- 完了条件: 本番URLで自分が通しで使え、ログがSupabaseに落ちている

### Step 4: 知人テスト（1〜2週間、開発と並行可)
- 5〜10人に個別に送る（一斉ではなく1人ずつ。テンプレ感を出さない）
- 可能なら2〜3人は画面共有 or 対面で操作を観察（どこで指が止まるかが最大の情報）
- 聞くことは3つだけ:
  1. 入力で詰まった場所はどこか
  2. 表示された金額に納得感はあるか（数字が怪しいと思った瞬間はどこか）
  3. 月5,000円の継続版に「何があれば」払うか
- 完了条件: 完走率とヒアリングメモが揃い、改良の優先順位が3つ決まる

### Step 5: 改良サイクル
- フィードバック反映 → 同じ人にもう一度使ってもらう → 反応が変わるか確認
- ここから先（メールでの詳細レポート、X/note での公開、課金）は検証結果を見て判断

## Phase 2 バックログ（有料版の核 = 「算定タスクを手助けするツール」）

診断は「何をすれば取れるか」までを示す。有料版はその行為（タスク）自体を手助けする:

1. **トレーシングレポート作成支援**: SBAR形式のテンプレート + 入力補助。服薬情報等提供料の
   算定と地域支援系の実績要件（年間回数）に直結。※患者情報を扱うため匿名化入力の設計が必須
2. **実績カウンタ**: 在宅件数・TR回数・かかりつけ同意数を月次で記録し、区分アップまでの
   残り件数と必要ペースを表示（案Cのタスク管理画面）
3. **算定漏れチェックリスト**: ハイリスク薬・麻薬・吸入・残薬調整・疑義照会後の算定を
   日次でチェックする運用ツール
4. **届出書類の下書き支援**: ベースアップ評価料・電子的調剤情報連携などの届出様式ガイド
5. **疑義解釈のAI要約配信**: 厚労省Q&Aを自店の状況に合わせて要約（ここで初めてLLMを使う）

診断対象外のまま保留（ユーザー判断待ち）: 在宅系一式、調剤技術系（無菌・自家製剤・計量混合）の深掘り。

## 5. リスクと対策

| リスク | 対策 |
|---|---|
| 点数・要件の誤り | 全加算に根拠リンクと lastReviewed。免責ページ。「概算」表記を徹底 |
| 「コンサル」とみなされる | 情報提供に徹し、断定文言を避ける。算定判断は厚生局確認を促す |
| 入力が面倒で離脱 | 冒頭は「算定中のもの」チェック1画面+質問15問・約3分。「わからない」選択肢と業界平均のデフォルト値 |
| 「要件充足=算定中」の混同 | 算定状況は自己申告アンケートで取得し、「算定中/今すぐ取れる/要件を整えれば取れる」を分離 |
| 金額が大きすぎて嘘くさい | 計算根拠（点数×枚数）を結果画面で開示。盛らない |
